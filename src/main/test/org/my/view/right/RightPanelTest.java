package org.my.view.right;
import org.junit.Test;

/**
 * Created by paramonov on 23.08.17.
 */
public class RightPanelTest {

    @Test
    public void testRequest() throws Exception {
       /* LocaleManager.get().loadBundle("org.my.l18n.transl");
        LocaleManager.get().setDefaultBundleName("org.my.l18n.transl");
        RightPanel testPanel = new RightPanel();
        final RequestSourcePanel requestSourcePanel = (RequestSourcePanel)UiHelper.getChildNamed(testPanel, RequestSourcePanel.class.getCanonicalName());
        final JButton sendButton = requestSourcePanel.getSendButton();
        final JEditorPane requestHeadersTxt = requestSourcePanel.getRequestHeadersPane();

        requestHeadersTxt.setText(HttpRequestTest.MIN_REQUEST.toString());
        requestSourcePanel.getRequestBodyPane().setText("Request");

        SendRequestAction action = new SendRequestAction(requestSourcePanel, param -> new TestRequestWorker((HttpRequest) param));
        sendButton.setAction(action);
        sendButton.doClick();
        //final HttpResponse httpResponse = action.getWorker().get();
        //System.out.println(httpResponse.getBody());
        Thread.sleep(2000); //TODO: await SendRequestAction fire event

        final ResponseSourcePanel responseSourcePanel = (ResponseSourcePanel) UiHelper.getChildNamed(testPanel, ResponseSourcePanel.class.getCanonicalName());
       assertEquals("Request + Response", responseSourcePanel.getResponseBodyTextPane().getText());*/


    }


}